# Vercel Environment Variables - Created on 2024-12-19
# Copy these variables to your Vercel Dashboard Environment Variables section

# Database Configuration
MONGODB_URI=your_mongodb_connection_string_here

# Environment
NODE_ENV=production

# Authentication
JWT_SECRET=your_strong_jwt_secret_for_production_change_this

# Email Service Configuration
SENDGRID_API_KEY=your_sendgrid_api_key_here
FROM_EMAIL=your_email@example.com
FROM_NAME=Your Name
REPLY_TO_EMAIL=your_email@example.com

# Application URLs (Update after deployment)
API_BASE_URL=https://your-app-name.vercel.app
APP_BASE_URL=https://your-frontend-domain.com

# Email Limits
DAILY_EMAIL_LIMIT=1000
HOURLY_EMAIL_LIMIT=100
BATCH_SIZE=50

# Rate Limiting
RATE_LIMIT_WINDOW=15
RATE_LIMIT_MAX_REQUESTS=100

# Vercel Specific
VERCEL=1
VERCEL_URL=${VERCEL_URL}

# Instructions:
# 1. Go to your Vercel project dashboard
# 2. Navigate to Settings > Environment Variables
# 3. Add each variable above (except comments)
# 4. Update JWT_SECRET with a strong random string
# 5. Update API_BASE_URL and APP_BASE_URL after deployment
# 6. DO NOT commit this file to Git - add to .gitignore